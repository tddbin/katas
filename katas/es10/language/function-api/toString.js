// ES10 - 8: `function.toString()`
// To do: make all tests pass, leave the assert lines unchanged!
// Follow the hints of the failure messages!


describe('`function.toString()` - now returns the complete source code of the function', () => {
  // to do
});
