// ES10 - 5: array.flat
// To do: make all tests pass, leave the assert lines unchanged!
// Follow the hints of the failure messages!

describe('`array.flat()` creates an array with all sub-array elements concatenated recursively', () => {
  // to do
});
