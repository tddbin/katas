// ??: Map - initialize from iterator
// To do: make all tests pass, leave the assert lines unchanged!

describe('initialize a `Map` from an iterator', function(){

  it('', function() {
    const mapSize = new Map();
    // assert.equal(mapSize, 0);
  });

});
