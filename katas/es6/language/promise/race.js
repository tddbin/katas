// ??: Promise - race
// To do: make all tests pass, leave the assert lines unchanged!

xdescribe('`promise.race()` returns as soon as one of the given promises resolves or rejects', () => {

  it('two resolving', () => {
    
  });
  it('one rejecting, other resolving delayed, etc.', () => {
    
  });
});