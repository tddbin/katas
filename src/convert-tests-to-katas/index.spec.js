// @flow
import {describe, it} from 'mocha';

describe('Convert tests to katas', () => {
  it('FIRST filter the `sourceDir` for kata files', () => {

  });
  it('THEN converts the list of kata files to destination-pairs', () => {

  });
  it('THEN creates all missing destination directories', () => {

  });
  it('THEN katafies all tests', () => {

  });
});